import { RevenueSimulatorClient } from "@/components/admin/RevenueSimulatorClient";

export const metadata = { title: "Simulador de Ingresos · Newayzi Admin" };

export default function SimulatorPage() {
  return <RevenueSimulatorClient />;
}
